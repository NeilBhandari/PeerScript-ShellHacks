import React from 'react';
import ReactDOM from 'react-dom';
import CreatePatient from "./Create-New-Patient.jsx";
import './Create-New-Patient.css'

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <CreatePatient />
    </React.StrictMode>
);
