import React from 'react';
import ReactDOM from 'react-dom';
import InfoDisplay from "./Info-Display.jsx";
import './Info-Display.css';

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>

        <InfoDisplay />
    </React.StrictMode>
);
